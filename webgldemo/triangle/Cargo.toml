[package]
name = "shadegldemo"
version = "0.0.2"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
dataview = "1.0"
cvmath = { version = "0.0.1", features = ["dataview"] }
shade = { path = "../..", features = ["webgl"] }
